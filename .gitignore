# Secrets / keys
.env
.env.*
!.env.example
.keys/
*.pem

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/
build/
dist/

# Arena runtime state.
# A committed snapshot of evidence/live/ ships as part of the evidence pack (a real, signed
# live-mode tournament served at GET /live); `make live` advances it locally on fresh Bitget data.
.runtime/

.vercel
